import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'issue_cap_739',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
